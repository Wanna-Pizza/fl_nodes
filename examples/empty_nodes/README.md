Empty Nodes example

This minimal example shows only the node editor with nodes that have a single editable field and a simple context menu (right-click).

To run:

```bash
cd examples/empty_nodes
flutter pub get
flutter run
```

It depends on the local `fl_nodes` package in this repository.
